#!/usr/bin/env python

from distutils.core import setup

setup(name='remote_exec',
      version='1.0',
      description='IPython magics to run code remotely',
      author='Mike Boyle',
      url='https://github.com/moble/remote_exec',
      py_modules = ['remote_exec',],
)
